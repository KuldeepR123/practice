package com.example.string;

import java.util.Scanner;

public class Demo {
   /* private static final int divisor = 3;
    public static void main(String number){
        if(number.equals("")){
            return;
        }
        int length = number.length();
        int value = length%3;
        Long n = Long.parseLong(number);
        if(value==0){
            System.out.println(number.substring(0,divisor));
            number = number.substring(divisor);
            Demo.main(number);
        }
        else{
            System.out.println(number.substring(0,value));
            number = number.substring(value);
            Demo.main(number);
        }
    }*/
    public static void main(String[] args) {
      /*  Scanner scan = new Scanner(System.in);
        Long n = scan.nextLong();
        Demo.main(Long.toString(n));*/
    }
}
